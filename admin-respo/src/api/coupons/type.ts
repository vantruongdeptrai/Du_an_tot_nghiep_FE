export type Coupon = {
    id?: string;
    name: string;
    description: string;
    discount_amount: number;
}

export type CouponInput = {
    id?: string;
    name: string;
    description: string;
    discount_amount: number;
}